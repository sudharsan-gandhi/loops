import {
  Box,
  Header,
} from '@adminjs/design-system';

const Login: React.FC = () => {
  return (
    <div>
        <Header >Login</Header>
      <Box>New Login Page</Box>
    </div>
  );
};

export default Login;
