import { ComponentStyle } from '../interface';

export const Button: ComponentStyle = {
    defaultProps: {
        variant: 'outline',
        colorScheme: 'red'
    }
};


export const NavItem: ComponentStyle = {
    defaultProps: {
        variant: 'outline',
        colorScheme: 'red'
    }
}